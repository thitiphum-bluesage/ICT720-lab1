This DataCollection Directory is created using platformio to collect temperature and huminity using CucumberRS with hts221 sensor at thitiphum's enviroment (my bedroom)

~~For now this board is only doing log them and not store to real-time database yet~~

This board collects data and stores it in a real-time database every 20 seconds periodically.